name: Génération ODS depuis RSS

on:
  # Exécution manuelle uniquement
  workflow_dispatch:
    inputs:
      date_from:
        description: "📅 Date de début (JJ/MM/AAAA) — laisser vide pour tout récupérer"
        required: false
        default: ""
      date_to:
        description: "📅 Date de fin (JJ/MM/AAAA) — laisser vide = aujourd'hui"
        required: false
        default: ""
      max_articles:
        description: "📊 Nombre max d'articles (laisser vide = tous)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Générer le fichier ODS
        run: |
          mkdir -p output
          CMD="python rss_to_ods.py -o output/uneiaparjour.ods"
          if [ -n "${{ github.event.inputs.date_from }}" ]; then
            CMD="$CMD --from '${{ github.event.inputs.date_from }}'"
          fi
          if [ -n "${{ github.event.inputs.date_to }}" ]; then
            CMD="$CMD --to '${{ github.event.inputs.date_to }}'"
          fi
          if [ -n "${{ github.event.inputs.max_articles }}" ]; then
            CMD="$CMD --max ${{ github.event.inputs.max_articles }}"
          fi
          echo "🚀 $CMD"
          eval $CMD

      - name: Commit et push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/uneiaparjour.ods
          git diff --cached --quiet || git commit -m "🔄 Mise à jour ODS — $(date -u +'%d/%m/%Y %H:%M UTC')"
          git push
